name: hello
description: Wallpaper app for sharing images

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity: ^2.0.0
  cupertino_icons: ^0.1.3
  flutter_spinkit: ^4.1.2+1
  fzregex: ^1.0.3
  google_fonts: ^1.1.1
  http: ^0.12.2
  provider: ^4.3.2+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_rename_app: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
